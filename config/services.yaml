services:
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.
        public: true

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    SymfonyRbacBundle\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Exception/'
            - '../src/Repository/'

    SymfonyRbacBundle\EventSubscriber\AccessControlDriver:
      tags:
        - { name: kernel.event_listener, event: onKernelController }

    SymfonyRbacBundle\Repository\PermissionRepository:
        arguments:
            $entityClass: '%symfony_rbac.resolve_target_entities.permission%'

    SymfonyRbacBundle\Repository\RoleRepository:
        arguments:
            $entityClass: '%symfony_rbac.resolve_target_entities.role%'
